 // 获取文件对象并转换成base64
 export const fileToBase64 = (url) => {
 	// 获取到文件对象
 	console.log(url);
 	// import file from url
 	// console.log(file);
 	// // let file =
 	// // 获取FileReader实例
 	var reader = new FileReader()
 	// // 将文件加载进入
 	// reader.readAsDataURL(file)
 	// reader.onload = function(e) {
 	// 	// 转换完成输出该文件base64编码
 	// 	console.log(e)
 	// }
 	// return e
 }